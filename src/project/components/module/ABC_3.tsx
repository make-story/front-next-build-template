import React, { useEffect } from 'react';

import withModule from './withModule';

const ABC_3 = () => {
  useEffect(() => {
    // 데이터 호출
  }, []);

  return <>ABC_3</>;
};

export default withModule(ABC_3);
