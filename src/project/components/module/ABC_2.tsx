import React, { useEffect } from 'react';

import withModule from './withModule';

const ABC_2 = () => {
  useEffect(() => {
    // 데이터 호출
  }, []);

  return <>ABC_2</>;
};

export default withModule(ABC_2);
