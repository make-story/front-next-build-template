import React, { useEffect } from 'react';

import withModule from './withModule';

const ABC_5 = () => {
  useEffect(() => {
    // 데이터 호출
  }, []);

  return <>ABC_5</>;
};

export default withModule(ABC_5);
