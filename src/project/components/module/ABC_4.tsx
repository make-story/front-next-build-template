import React, { useEffect } from 'react';

import withModule from './withModule';

const ABC_4 = () => {
  useEffect(() => {
    // 데이터 호출
  }, []);

  return <>ABC_4</>;
};

export default withModule(ABC_4);
